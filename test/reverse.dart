/// void reverse()
/// 
/// Метод ничего не принимает и ничего не возвращает.
/// Надо проверить что он сделает со списком == null, из одного элемента, из двух элементов,
/// из трёх элементов, из четырёх, из чётного числа элементов больше четырёх и из нечётного числа элементов больше трёх.
